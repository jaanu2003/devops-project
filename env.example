# Copy to your shell before running scripts (do not commit real .pem paths to git)

# Required for scripts/generate_inventory.sh and full_deploy.sh
SSH_KEY_PATH=E:\Jahnavi\Main\devops-project\devops-key.pem
ANSIBLE_USER=ubuntu

# Optional: use instead of terraform output
EC2_HOST=13.126.236.86

# Optional: full_deploy.sh — set true to run terraform apply first
RUN_TERRAFORM=false

# Ansible / Jenkins deploy branch and repo
GIT_REPO_URL=https://github.com/jaanu2003/devops-project.git
APP_DEPLOY_BRANCH=main
AWS_DEFAULT_REGION=ap-south-1
